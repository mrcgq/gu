<!-- =============================================================================
     文件: Controls/SpeedGauge.xaml
     描述: 实时速度仪表盘控件
============================================================================= -->
<UserControl x:Class="PhantomGUI.Controls.SpeedGauge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">
    
    <Grid>
        <lvc:CartesianChart DisableAnimations="True" 
                            Hoverable="False"
                            DataTooltip="{x:Null}">
            <lvc:CartesianChart.Series>
                <!-- 上传速度线 -->
                <lvc:LineSeries Title="上传"
                                Values="{Binding UploadHistory}"
                                Stroke="#4CAF50"
                                Fill="Transparent"
                                StrokeThickness="2"
                                PointGeometry="{x:Null}"/>
                
                <!-- 下载速度线 -->
                <lvc:LineSeries Title="下载"
                                Values="{Binding DownloadHistory}"
                                Stroke="#2196F3"
                                Fill="Transparent"
                                StrokeThickness="2"
                                PointGeometry="{x:Null}"/>
            </lvc:CartesianChart.Series>
            
            <lvc:CartesianChart.AxisX>
                <lvc:Axis ShowLabels="False" 
                          Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
            </lvc:CartesianChart.AxisX>
            
            <lvc:CartesianChart.AxisY>
                <lvc:Axis LabelFormatter="{Binding YFormatter}"
                          MinValue="0"/>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</UserControl>



